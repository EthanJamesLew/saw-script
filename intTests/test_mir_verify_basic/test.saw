enable_experimental;

let id_u32_spec = do {
  x <- mir_fresh_var "x" mir_u32;

  mir_execute_func [mir_term x];

  mir_return (mir_term x);
};

m <- mir_load_module "test.linked-mir.json";
mir_verify m "test::id_u32" [] false id_u32_spec z3;
